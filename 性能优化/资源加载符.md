`preconnect`: 
- 提前握手，提高网页的首屏加载性能
- 完成DNS + TCP + TLS, 全流程建立连接，
- 适用于 关键第三方域名（不能滥用 建立连接需要消耗资源）
  
- 使用时机：字体库、核心API、CDN静态资源， 控制数量 建议不超过 6个

`dns-prefetch`:
- 仅提前解析dns, 不建立连接
- 非关键的第三方资源（分析脚本、插件等）
- 可以作为preconnect的降级方案

`preload`:
- 提前触发关键资源的下载
- 背景图、首屏必需的动态脚本

`prefetch`:
- 当页面加载完后 在空闲时间请求资源