/**
 * Vue.component 作用就是 手机全局定义，id 和 
 */
//组件的渲染
/**
 * 1、先将模板编译成 render函数
 * 2、创建组件的虚拟节点
 * new CTOr 挂载
 * isSameVnode
 * 创建真实节点 patch createElement
 * 也要区分 是组件还是元素
 * vnode ={ data , hook ,}
 * 保存组件的实例到 vnode 上
 */
